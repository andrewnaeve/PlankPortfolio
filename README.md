#Portfolio site for N. Plank

E-commerce site built from scratch with the Square api, painstakingly without Redux. Instead, opts for custom context providers built with React's context api, and render props.
